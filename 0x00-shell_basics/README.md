#!/bin/bash specifies program to write script is bash
ls -lan Displays current directory contents, including hidden files (starting with .). Use the long format with user and group IDs displayed numerically 
